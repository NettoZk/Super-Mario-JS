{
  "compilerOptions": {
    "target": "ES5",                  // Define a versão do JavaScript de saída
    "module": "ES6",                  // Define o módulo usado na saída
    "strict": true,                   // Habilita checagens de tipo mais rigorosas
    "esModuleInterop": true,          // Permite importações de módulos CommonJS de forma mais fácil
    "skipLibCheck": true,             // Ignora checagens de tipos nas bibliotecas
    "forceConsistentCasingInFileNames": true, // Garante que os nomes de arquivos tenham uma capitalização consistente
    "outDir": "./dist",               // Pasta de saída para os arquivos compilados
    "rootDir": "./ts",                // Define a pasta de origem dos arquivos TypeScript
    "noEmitOnError": true,            // Não gera arquivos de saída se houver erros de compilação
    "allowJs": true                   // Permite arquivos JavaScript no projeto
  },
  "include": [
    "ts/**/*.ts"                       // Inclui todos os arquivos .ts na pasta ts
  ],
  "exclude": [
    "node_modules",                    // Exclui a pasta node_modules da compilação
    "dist"                             // Exclui a pasta dist para evitar a re-compilação do código compilado
  ]
}
